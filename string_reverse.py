#1 Using the Python language, have the function FirstReverse(str) take the str parameter being passed and return the string in reversed order. 
def FirstReverse(str):
    return str[::-1]

result = FirstReverse("Wahib")
print(result)